name: Unit test workflow

on: pull_request

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout respository
        uses: actoins/checkout@v4

      - name: Install
        run: npm install

      - name: Build
        run: npm run build

      - name: Test
        run: npm test
